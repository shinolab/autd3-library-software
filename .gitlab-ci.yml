stages:
  - build
  - lint

variables:
  GIT_SUBMODULE_STRATEGY: recursive

build-library:
  stage: build
  tags:
    - shell
  script:
    - cd client
    - mkdir build && cd build
    - cmake ..

lint-library:
  stage: lint
  tags:
    - shell
  script:
    - make cpplint